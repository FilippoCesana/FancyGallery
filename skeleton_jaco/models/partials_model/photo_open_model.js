//data per una foto quando viene cliccata all'interno di un evento

const photo_open_model = {
    dataURL      : "dataURL",
    photographer : "username_photographer",
    timestamp    : "timestamp",
    id           : "id"
}